{
  "name": "buffela",
  "version": "1.0.0",
  "description": "",
  "private": "true",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "publish": "npm run build -ws && npm publish -ws"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^24.10.13",
    "tsup": "^8.5.0",
    "typescript": "^6.0.3"
  },
  "dependencies": {
    "protobufjs": "^8.0.0"
  }
}
